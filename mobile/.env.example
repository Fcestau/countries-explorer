# EXPO_PUBLIC_ vars are inlined into the client bundle at build time — there is no
# server tier on mobile, so this key is visible to anyone who inspects the app bundle.
# Get a free key at https://restcountries.com/docs/api-versions (500 req/month).
EXPO_PUBLIC_RESTCOUNTRIES_API_KEY=
